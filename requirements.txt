# =============================================================
# Multimodal AI Assistant Project Requirements
# 锁定主版本，避免随机升级导致环境崩溃
# 完整安装流程参考 docs/项目总执行计划.md §8 + §13
# =============================================================

# ============================================================
# 深度学习核心
# ============================================================
# 注意：torch 应按 CUDA 版本单独安装，参考 https://pytorch.org/get-started/locally/
# 例如：pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.4.0
transformers>=4.57.0
tokenizers>=0.20.0
accelerate>=0.34.0
peft>=0.13.0

# ============================================================
# 多模态处理（text + image + video）
# ============================================================
qwen_vl_utils>=0.0.14
Pillow>=10.0.0
opencv-python>=4.9.0
imageio>=2.34.0
imageio-ffmpeg>=0.5.0
av>=11.0.0
decord>=0.6.0

# ============================================================
# 训练框架（ms-swift）
# ============================================================
ms-swift>=4.0.0

# ============================================================
# GRPO 数学验证
# ============================================================
math_verify>=0.6.0

# ============================================================
# 推理（vLLM）
# ============================================================
vllm>=0.6.0

# ============================================================
# 数据处理
# ============================================================
datasets>=2.20.0
pyarrow>=15.0.0
parquet>=1.0.0
huggingface-hub>=0.25.0

# ============================================================
# Agent + LLM 客户端
# ============================================================
openai>=1.50.0
langchain>=0.3.0
langgraph>=0.2.0
langchain-openai>=0.2.0
langchain-community>=0.3.0

# ============================================================
# FastAPI 后端服务（V1+）
# ============================================================
fastapi>=0.110.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.5.0
pydantic-settings>=2.5.0
websockets>=12.0

# ============================================================
# 数据库（PostgreSQL + Redis）
# ============================================================
SQLAlchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.9
asyncpg>=0.29.0
redis>=5.0.0
hiredis>=3.0.0

# ============================================================
# JWT 认证（auth + refresh token + WebSocket ticket）
# ============================================================
PyJWT>=2.8.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ============================================================
# 工具库
# ============================================================
requests>=2.32.0
httpx>=0.27.0
pyyaml>=6.0.1
tqdm>=4.66.0
rich>=13.7.0
python-dotenv>=1.0.1
tenacity>=8.3.0

# ============================================================
# 开发 / 测试
# ============================================================
pytest>=8.0.0
pytest-asyncio>=0.23.0
black>=24.0.0
ruff>=0.5.0
ipython>=8.20.0
mypy>=1.10.0

# ============================================================
# 可选：实验跟踪（取消注释启用）
# ============================================================
# wandb>=0.17.0
# swanlab>=0.4.0
# tensorboard>=2.16.0

# ============================================================
# 可选：量化（如需 QLoRA）
# ============================================================
# bitsandbytes>=0.43.0

# ============================================================
# 可选：分布式训练
# ============================================================
# deepspeed>=0.14.0

# ============================================================
# 客户端依赖（说明，非 Python 包）
# ============================================================
# Mobile App (clients/mobile-app/)：
#   - expo
#   - react / react-native
#   - typescript
#   - expo-secure-store      (JWT 加密存储)
#   - expo-image-picker      (图片选择)
#   - eas-cli                (Android APK 构建)
#
# WeChat Mini Program (clients/wechat-mini/)：
#   - @tarojs/cli
#   - @tarojs/react
#   - react / typescript
#   - miniprogram-ci         (代码上传)
#
# 共享层 (packages/)：
#   - api-contract           (TypeScript API DTO)
#   - chat-protocol          (WebSocket 消息协议类型)
#
# 部署（infra/）：
#   - nginx (system package)
#   - postgresql (system package, ≥ 15)
#   - certbot (system package, Let's Encrypt)
#   - systemd (内置)